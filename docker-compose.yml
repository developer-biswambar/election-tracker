services:
  election-monitor:
    build: .
    restart: unless-stopped
    env_file: .env
    environment:
      - SNAPSHOT_PATH=/data/snapshot.json
    volumes:
      - election-data:/data

volumes:
  election-data:
